### Changed
- Replaced technical `Admin/Oper` labels in switch ports with user-friendly terms:
  - `Shut / No Shut`
  - `Link Status`
- Manual port status selector now follows the same wording.

### Added
- Added `SFP Absent` link state for ports where transceiver/module is missing.

### Fixed
- Improved SNMP oper-status parsing so `absent/notPresent` is separated from normal `down` state.
