## Fixed
- Cron Health in Dashboard and Settings now correctly detects task success using both log styles:
  - `task:<name> completed`
  - legacy messages like `poll_usage processed`, `enforce_queue executed`, `graph_warm executed`, `cleanup executed`, `switch_discovery executed`, `self_update executed`
- Resolved false `Fail` status for cron tasks immediately after dispatcher runs.
