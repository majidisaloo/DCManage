## Fixed
- Resolved fatal error in Settings/Cron monitor on servers with `escapeshellarg()` disabled.
- Added robust internal shell quoting fallback for cron command hints.
- Kept real-path cron command rendering without dependency on blocked shell helper functions.
