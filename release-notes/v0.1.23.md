### Added
- Added switch port speed column in Switches -> Ports table.
- Discovery now stores:
  - `speed_mbps` from `ifHighSpeed` (fallback `ifSpeed`)
  - `speed_mode` using auto-negotiation status

### Changed
- Port speed labels now show clear operational state:
  - `1G`, `10G`
  - `Auto 1G`, `Auto 10G`

### Fixed
- Fixed missing speed/mode recognition for discovered switch ports.
