## Changed
- Persian menu/tabs now render RTL from the right side consistently.
- `PRTG` tab is renamed to `Monitoring` and prepared for multi-platform selection.
- Datacenter list layout and action column were reorganized for cleaner alignment.

## Added
- Monitoring provider dropdown with options: `PRTG`, `Cacti`, `SolarWinds`.
- Datacenter row actions: `Racks`, `Networks`, `Servers`, `Edit`, `Delete`.
- Rack U-map enhancements with manual unit markers: `Reserved`, `Cable Mgmt`, `Airflow`, `Blank`.
- New migration/table: `mod_dcmanage_rack_units` for persistent rack-unit annotations.

## Fixed
- Dashboard update/version panels no longer fail due to malformed API endpoint URL patterns.
- Dashboard async API handling now catches invalid JSON responses and avoids unhandled promise rejections.
- Datacenter server detail rows now display rack names (instead of raw rack IDs).
- Duplicate page heading/version noise outside dashboard flow was removed.
