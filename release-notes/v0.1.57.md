## Changed
- Live search now normalizes Persian/Arabic digits and common Arabic/Persian letter variants to improve mixed-language matching.
- Servers table pagination moved below table and now defaults to `15` rows with size selector (`10/15/25/50/100`).

## Fixed
- Server add form now requires datacenter selection before rack/switch/port become selectable.
- Rack and switch options are now strictly filtered by selected datacenter before port loading.
