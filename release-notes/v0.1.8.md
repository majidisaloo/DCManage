## Changed
- Settings field `Locale` was renamed to `Language`.
- Language selector now provides three explicit choices: `Default`, `English`, `Persian`.

## Fixed
- `Default` language mode now follows WHMCS admin/session language correctly.
- Language setting save flow now validates accepted values (`default`, `en`, `fa`) to avoid invalid configuration.
