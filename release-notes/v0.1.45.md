## Changed
- Removed standalone iLO tab from top navigation and moved iLO management into Servers workflow.
- Added iLO fields directly in Server create/edit forms (host/user/type/password).

## Added
- Added Server delete action with related port/sensor cleanup.
- Added iLO proxy settings in Settings (type/host/port/user/password).
- Added iLO proxy connection test action supporting `http`, `https`, and `socks5`.

## Fixed
- Fixed server edit JS crash (`SyntaxError: Invalid escape in identifier`) caused by malformed inline script escaping.
- Server edit/details panel now opens correctly and actions execute without JS parse failure.
