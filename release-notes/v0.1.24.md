### Changed
- Removed `Ports` tab from the module navigation.
- Removed standalone `Ports` dashboard card.

### Fixed
- Requests using old `&tab=ports` links now open `Switches` automatically.

### Added
- Port management remains fully available under `Switches` (single consolidated workflow).
