Changed
- Language direction is now dynamic: Persian UI renders right-to-left and English UI renders left-to-right.
- Main menu order was rebuilt to start from Dashboard and end at Logs.
- Dashboard now acts as an operations center with direct links from KPI cards to related sections.

Added
- Added Datacenter creation flow with automatic rack generation (count + per-rack unit size).
- Added Switch creation form with Datacenter -> Rack dependent selection.
- Added Server creation form with Datacenter -> Rack dependent selection and U position fields.
- Added Cron monitor in Settings with task command, status, last run, next run, and overall health.
- Added Dashboard cron health widget with green/yellow/red status logic.
- Added package purchase logs in Logs tab.

Fixed
- Fixed locale defaults to follow detected session/system language instead of static fallback.
- Added missing bilingual labels for newly introduced UI and controls.
- Improved field styling and visual hierarchy for a more modern settings/forms experience.
