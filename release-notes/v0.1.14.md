## Changed
- Removed `Automation / Cron` tab from top navigation; cron monitor remains under `Settings`.
- Queue KPI card now opens `Settings` to keep cron/automation controls centralized.

## Fixed
- Cron command hints now show auto-detected real filesystem path for `cron.php` (no placeholder path).
- PHP CLI binary path in cron hints is auto-detected from server environment.
