### Changed
- Switch port row actions now use operational controls: `Shut` and `No Shut` (delete button removed).
- Settings now include `Switch Discovery Interval (Minutes)` to control periodic SNMP auto-discovery timing.

### Added
- Added automatic `switch_discovery` cron task with lock safety and interval-aware execution.
- Added interface description (`ifAlias`) and interface index persistence for discovered switch ports.
- Added `switch_discovery` to cron monitor output and generated cron command hints.

### Fixed
- Fixed VLAN recognition by mapping `dot1qPvid` through `dot1dBasePortIfIndex` to the correct interface.
- Improved SNMP discovery compatibility so VLAN/description extraction works across different SNMP function availability.
