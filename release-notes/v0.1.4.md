Changed
- GitHub update source is now hardcoded and no longer exposed as editable repo/branch settings.
- Dashboard now includes a dedicated version center showing installed version and latest release.
- Module tab navigation received a colored visual style for faster recognition.

Added
- Added one-click `Check Update` action in Dashboard.
- Added one-click `Apply Update` action in Dashboard.
- Added direct Auto Update toggle in Dashboard.
- Added `Settings` tab for global module configuration (timezone, locale, polling/caching/retention/refresh).
- Added update API endpoints for check/apply/auto-toggle flows.

Fixed
- Removed dependency on user-editable GitHub source parameters for update consistency.
- Update copy process now skips non-essential artifacts.
