## Fixed
- Update banner no longer stays on stale `Canceled before HTTP request` after update completion; idle runtime state now clears canceled state automatically.
- Cron health status now evaluates success by task age window after matching valid success patterns, not by requiring `completed` text only.
- Dashboard and Settings cron monitors now stay consistent for legacy cron messages (`... executed`, `... processed`).
- Settings iLO proxy action row spacing fixed (`Save Settings` / `Test iLO Proxy`) with consistent layout and gaps.
