### Changed
- Removed duplicated in-page section titles that repeated active tab names.
- `Switches` page top row now uses action-only layout (right-aligned `Add Switch`) without extra title text.

### Fixed
- Repeated `Switches/Servers/Monitoring` labels (tab + content) are no longer shown.
