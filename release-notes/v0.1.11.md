## Fixed
- Resolved dashboard `Invalid API response` errors by removing PHP 8-only syntax from API/runtime paths.
- Restored dashboard widgets and controls that depend on API JSON (`health`, `version/update`, `cron`).
- Update buttons are now visible and functional again when dashboard loads on older PHP environments.
