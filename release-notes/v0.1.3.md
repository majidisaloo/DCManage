Changed
- Release notes are now published in a structured format (`Changed / Added / Fixed`) instead of generic auto-generated changelog text.
- Release package behavior remains WHMCS-ready and keeps the module placement path compatible with direct extraction in `public_html`.

Added
- Version-specific release-notes pipeline: each tag can ship a dedicated release note body file.
- Fallback release-note template in CI for tags that do not yet have an explicit note file.

Fixed
- Resolved inconsistent GitHub Release presentation where only `Full Changelog` link was shown.
- Standardized the human-readable release summary for easier operational review before production deployment.
