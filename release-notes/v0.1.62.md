## v0.1.62

### Added
- Implemented full `Traffic Packages` CRUD in admin tab.
- Added package creation/edit with required datacenter scope.
- Added bilingual labels for package fields/actions (`EN`/`FA`).

### Changed
- `Traffic Packages` tab now renders functional UI instead of placeholder text.
- Package fields: Datacenter, Package Name, Traffic Unit (GB), Price, Taxed, Status.
