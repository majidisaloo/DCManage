### Added
- Server form now supports:
  - switch selection
  - switch port selection
  - PRTG instance selection
  - multi-select traffic sensors
- Added `prtg/sensors` API endpoint to load sensors from selected PRTG instance.
- Monitoring tab now has PRTG instance management (create/list/test/delete).
- Added `mod_dcmanage_server_traffic_sensors` table for multiple sensor mappings per server.

### Changed
- Servers list now displays linked switch/port and sensor count preview.

### Fixed
- Server creation now stores network/traffic mappings directly during provisioning.
