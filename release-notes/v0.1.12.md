## Fixed
- Dashboard/API parsing now works even when WHMCS wraps addon output with admin HTML.
- Added explicit API JSON markers and frontend fallback extraction logic.
- Restored `Version/Update` and `Cron` dashboard cards/buttons where `Invalid API response` was shown.
