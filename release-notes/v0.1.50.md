# v0.1.50

## Fixed
- Fixed Cron Health false-fail in dispatcher mode by logging per-task completion events.
- Dispatcher now handles task failures per task and continues the remaining scheduled tasks.
