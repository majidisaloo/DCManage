## Fixed
- VLAN detection now supports SVI interfaces (`VlanX`, `vlanifX`) when PVID OID mapping is not available.
- Switch discovery and per-port check now apply VLAN fallback from interface name/description.
- VLAN value is also normalized/persisted during store phase to avoid empty VLAN column on compatible switch outputs.
