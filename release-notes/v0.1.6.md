Changed
- Removed duplicated module heading/version text outside Dashboard to keep a single clean header experience.
- Datacenters page switched to Add-button workflow with collapsed create form.
- Rack visualization now shows U-level occupancy blocks for each rack.

Added
- Added rack occupancy mapping using server/switch U positions.
- Added optional U position fields for switches.
- Added migration for switch U-position fields.

Fixed
- Fixed language resolution order to honor saved locale and then session/system fallback.
- Fixed language-mode rendering in JavaScript by binding to module container language state.
- Removed datacenter `Code` input dependency from create flow.
