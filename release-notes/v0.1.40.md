## Changed
- Port admin status wording is now `Active / Suspended`.
- Port action buttons are now `Activate` and `Suspend`.

## Added
- Added `Check Port` action per interface to run immediate one-port SNMP refresh.

## Fixed
- Improved absent-state recognition for additional raw variants including `abcent` and vendor-specific absent strings.
- `SFP/OSPF absent` style values now map to absent link status correctly.
