## Changed
- Switch Ports table is now rendered with a fixed column layout to keep all columns aligned even when description text length changes.
- Pagination controls for Switch Ports are now shown under the table (not above).
- Added a per-page selector in Switch Ports (`10/15/25/50/100`) with default `15`.
