## Fixed
- Update runtime state now clears stale error messages after successful update jobs.
- Dashboard no longer displays old `Update archive missing modules/addons/dcmanage` when latest update job is done.
- Runtime state is normalized to `updated` when installed version is already synced with latest cached release.
