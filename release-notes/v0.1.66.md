## Changed
- Added a shared inline action-bar style to normalize button spacing/alignment across module forms and tables.
- Standardized secondary action visuals so reset-like controls render as clear buttons (not plain text).

## Fixed
- Queue action area spacing fixed (`Apply Filter`, `Reset`, `Clear Done/Failed`) to avoid collapsed/stacked controls.
- Logs action controls moved to shared inline style for consistent margins and layout.
