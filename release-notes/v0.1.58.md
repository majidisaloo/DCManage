## Fixed
- Auto-update archive path detection now supports both release package layouts (root and nested top folder), resolving `Update archive missing modules/addons/dcmanage`.
- Cron `OK` badges were replaced with semantic green status pills in both Dashboard and Settings monitor.

## Changed
- Removed extra dashboard subtitle/version text block to keep the top section cleaner.
