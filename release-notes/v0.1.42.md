## Changed
- Server create dependency flow is now strict: Datacenter must be selected before rack/switch become usable.
- Switch-port loading now requires both Datacenter and Switch selection.

## Added
- Added hard disable/reset behavior for rack/switch/port fields when datacenter is empty.

## Fixed
- Fixed rack list being visible/selectable before selecting datacenter.
- Fixed switch port list not loading reliably after switch selection by enforcing DC+Switch guarded loading.
