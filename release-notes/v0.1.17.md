### Fixed
- Hardened cron command argument quoting to avoid fatal errors in environments where `escapeshellarg()` is restricted.
- Added a guarded callable fallback for shell quoting, so Settings and Cron Monitor pages continue to load on hardened hosts.
