### Added
- Added a shared switch-port discovery persistence helper for consistent auto-import behavior.
- SNMP Test now auto-discovers and stores switch interfaces when the switch is reachable via SNMP.

### Fixed
- Fixed wrong action routing in Switches forms by prioritizing button-bound actions (`dcmanage_action_btn`).
- Fixed false `Port interface name is required` errors by running auto-discovery fallback when interface input is empty.
- Discover/Update/Delete switch-port actions now execute reliably under WHMCS wrapped form contexts.
