# v0.1.46

## Changed
- Cron setup is now single-entry. You add one cron command and DCManage dispatcher runs all internal tasks on their own intervals.
- Settings Cron Monitor now displays one cron install command while preserving per-task status/last run/next run.

## Added
- Added dispatcher mode to `cron.php` and set it as default when no task argument is provided.
