name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build module package
        run: |
          set -euo pipefail
          mkdir -p dist
          cp -R modules/addons/dcmanage dist/dcmanage
          cd dist
          zip -r ../DCManage-${GITHUB_REF_NAME}.zip dcmanage

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            DCManage-${{ github.ref_name }}.zip
          generate_release_notes: true
          draft: false
          prerelease: false
